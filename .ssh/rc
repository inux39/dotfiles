#!/bin/bash
if [ "192" != "${SSH_CLIENT:0:3}" -a "fe80" != "${SSH_CLIENT:0:4}" ]; then
	echo -e "`date`\n\"$USER\" has logged on from $SSH_CLIENT" | mailx -s "SSH LOGIN ALERT" inux.0x27@gmail.com
fi

