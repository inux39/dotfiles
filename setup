#!/bin/sh
shellpath=$(cd $(dirname $0); pwd)
vimrc=$shellpath/vimrc
zshrc=$shellpath/zshrc
fonts=$shellpath/fonts.conf
echo "Select 0:Server 1:Client"
read -p ":" select

if [ $select == 1 ]; then
	#xprofile
	if [ ! -e $HOME/.xprofile ]; then
		ln -s $shellpath/.xprofile ~/
	fi
fi

# vimrc
if [ ! -e $HOME/.vim/.vimrc ]; then
	if [ ! -e $HOME/.vim ]; then
		mkdir $HOME/.vim
	fi
	ln -s $vimrc $HOME/.vim/
fi

# zshrc
if [ ! -e $HOME/.config/zsh/.zshrc ]; then
	if [ ! -e $HOME/.config/zsh ]; then
		mkdir $HOME/.config/zsh
	fi
	ln -s $zshrc $HOME/.config/zsh
	source $HOME/.config/zsh/zshrc
fi

# font config
if [ ! -e $HOME/.config/fontconfig ]; then
	ln -s $fonts $HOME/.config/fontconfig
fi

# vim bundle install
if [ ! -e $HOME/.vim/bundle ]; then
	curl https://raw.githubusercontent.com/Shougo/neobundle.vim/master/bin/install.sh | sh
fi

