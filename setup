#!/bin/sh
ShellPath=$(cd $(dirname $0); pwd)
configPath=$HOME/.config
zshPath=$HOME/.config/zsh
zshenv=.zshenv
zshrc=.zshrc
zprofile=.zprofile
# vimrc
if [ ! -e $HOME/.vim/vimrc ]; then
	if [ ! -e $HOME/.vim ]; then
		mkdir $HOME/.vim
	fi
	ln -sf $ShellPath/vimrc $HOME/.vim/vimrc
fi
# Zsh
if [ ! -e $HOME/$zshenv ]; then
	ln -sf $ShellPath/.config/zsh/$zshenv $HOME/$zshenv
fi

if [ ! -e $zshPath/$zshrc ]; then
	if [ ! -e $zshPath ]; then
		mkdir $zshPath
	fi
	ln -sf $ShellPath/.config/zsh/$zshrc $zshPath/$zshrc
	source $zshPath/$zshrc
fi
if [ ! -e $HOME/.xprofile ]; then
	cp -f $ShellPath/.xprofile $HOME/.xprofile
fi
# font config
if [ ! -e $configPath/fontconfig/fonts.conf ]; then
	ln -sf $ShellPath/.config/fontconfig/fonts.conf \
		$configPath/fontconfig/fonts.conf
fi

# gitconfig
if [ ! -e $HOME/.gitconfig ]; then
	ln -sf $ShellPath/.gitconfig $HOME/.gitconfig
fi

# vim bundle install
if [ ! -e $HOME/.vim/bundle ]; then
	curl https://raw.githubusercontent.com/Shougo/neobundle.vim/master/bin/install.sh | sh
fi

