#!/bin/sh
ShellPath=$(cd $(dirname $0); pwd)
mkdir -p $HOME/.config/zsh
ln -s $ShellPath/.ffmpeg $HOME/.ffmpeg
ln -s $ShellPath/.vim/vimrc $HOME/.vim/vimrc
ln -s $ShellPath/.config/zsh/.zshenv $HOME/.zshenv
# zsh
if [ ! -e $HOME/.config/zsh/.zshrc ]; then
	ln -sf $ShellPath/.config/zsh/.zshrc $HOME/.config/zsh/.zshrc
	source $HOME/.config/zsh/.zshrc
fi

# vimrc
if [ ! -e $HOME/.vim/autoload ]; then
	curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
	https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
fi

if [ ! -e $HOME/.xprofile ]; then
	cp -f $ShellPath/.xprofile $HOME/.xprofile
fi
if [ ! -e $HOME/.config/fontconfig/fonts.conf ]; then
	ln -sf $ShellPath/.config/fontconfig/fonts.conf \
		$HOME/.config/fontconfig/fonts.conf
fi
if [ ! -e $HOME/.gitconfig ]; then
	ln -sf $ShellPath/.gitconfig $HOME/.gitconfig
fi

