#!/bin/sh
ShellPath=$(cd $(dirname $0); pwd)
zshPath=$HOME/.config/zsh
zshenv=.zshenv
zshrc=.zshrc
zprofile=.zprofile
# vimrc
if [ ! -e $HOME/.vim/.vimrc ]; then
	if [ ! -e $HOME/.vim ]; then
		mkdir $HOME/.vim
	fi
	ln -sf $ShellPath/vimrc $HOME/.vim/.vimrc
fi
# Zsh
if [ ! -e $HOME/$zshenv ]; then
	ln -sf $ShellPath/zsh/$zshenv $HOME/$zshenv
fi

if [ ! -e $zshPath/.zprofile ]; then
	ln -sf $ShellPath/zsh/$zprofile $zshPath/$zprofile
fi
if [ ! -e $zshPath/$zshrc ]; then
	if [ ! -e $zshPath ]; then
		mkdir $zshPath
	fi
	ln -sf $ShellPath/zsh/$zshrc $zshPath/$zshrc
	source $zshPath/$zshrc
fi
if [ ! -e $HOME/.xprofile ]; then
	ln -sf $ShellPath/.xprofile $HOME/.xprofile
fi
# font config
if [ ! -e $HOME/.config/fontconfig ]; then
	ln -sf $ShellPath/fontconfig $HOME/.config/fontconfig
fi

# gitconfig
if [ ! -e $HOME/.gitconfig ]; then
	ln -sf $ShellPath/.gitconfig $HOME/.gitconfig
fi

# gitignore
if [ ! -e $HOME/.gitignore ]; then
	ln -sf $ShellPath/.gitignore $HOME/.gitignore
fi

# vim bundle install
if [ ! -e $HOME/.vim/bundle ]; then
	curl https://raw.githubusercontent.com/Shougo/neobundle.vim/master/bin/install.sh | sh
fi

